{
  "name": "onboarding",
  "version": "1.0.0",
  "description": "PTA-events",
  "main": "index.js",
  "scripts": {
    "test": "npx mocha tests/**/*.spec.js -r dotenv/config --timeout 60000",
    "smoke": "mocha tests/**/*smoke*.spec.js -r dotenv/config --timeout 90000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^2.0.0",
    "chai": "^4.3.7",
    "chai-subset": "^1.6.0",
    "chromedriver": "^114.0.3",
    "dotenv": "^16.3.1",
    "geckodriver": "^4.0.5",
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.10.0",
    "webdriver": "^8.13.1"
  }
}
